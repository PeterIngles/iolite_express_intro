# Generated by node so we don't need them in github
node_modules/

# Config file that you don't need to have on github, only on YOUR computer
.DS_Store

# Log files are not relevant to others, so no need to push them
*.log

# npm init --yes (for starting a new project)

# npm install express (can also uninstall)

# Semantic versioning ex 4.18.2 (Major, Minor, Patch) Needs to be the same for all team members

# killall -9 node (kills your server)

# npm start (runs the start in package.json)